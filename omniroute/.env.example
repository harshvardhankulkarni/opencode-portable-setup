# OmniRoute .env — sanitized example — copy to ~/.omniroute/.env and fill
# Original generated via omniroute bootstrap
STORAGE_ENCRYPTION_KEY=${STORAGE_ENCRYPTION_KEY} # generate: openssl rand -hex 32
